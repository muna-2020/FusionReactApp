/**
* @name GetMetaData
* @summary it returns the object of metadata
* @returns {array} MetaData
*/
export const GetData = () => {
    return {
        Percentage: 50, //progressbar percentage.
        ProgressText: "ProgressBarText" //Optional
    }
};