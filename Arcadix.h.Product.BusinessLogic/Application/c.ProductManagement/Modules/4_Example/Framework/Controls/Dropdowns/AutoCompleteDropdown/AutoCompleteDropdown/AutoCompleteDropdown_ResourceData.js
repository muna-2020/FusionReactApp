/**
 * @name GetResourceData
 * @summary retruns the water mark text value in object
 * */
export let GetResourceData = () => {
    let Text = {
        WaterMarkText: "search here"
    }
    return {
        Text
    }
}