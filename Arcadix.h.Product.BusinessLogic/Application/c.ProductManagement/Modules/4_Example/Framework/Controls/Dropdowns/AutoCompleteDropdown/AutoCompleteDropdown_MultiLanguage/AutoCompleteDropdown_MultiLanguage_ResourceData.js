/**
 * @name GetResourceData
 * @summary returns water mark text in object.
 * */
export let GetResourceData = () => {   
    let Text = {
        WaterMarkText: "search here" // place holder for control.
    }
    return {
        Text
    }
}